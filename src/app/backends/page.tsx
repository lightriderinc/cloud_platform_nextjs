import PagePlaceholder from "@/components/PagePlaceholder";

export default function BackendsPage() {
  return (
    <PagePlaceholder
      title="Backends"
      description="QPUs and simulators available to run your circuits."
    />
  );
}
